01一般项目中不设置外键约束，因为联动会影响效率。
02项目中主键类型一般不用int，因为主键会自增，影响效率。
    用UUID：会随机生成36位字符串，全世界唯一的。
03、mybatis的动态代理出现就是本身dao层下写实现类就不方便，mybatis动态代理解决了写dao接口的实现类，直接可以调用dao接口
04、namespace:要求与dao接口全限定名一致就是为了动态代理，
    还有dao接口名和mapper文件名一至。dao方法名和mapper标签id一致
05、mybatis配置文件中settings可以配置二级缓存，查询延迟加载等，不过一般不用，因为对于查询优化不明显。
06、遇到海量数据如何优化查询提高效率：
    1、基础操作：对常用查询字段设置索引。id自带索引
    2、高级操作：使用nosql数据库，redis
    3、专业操作：对于电商行业，搜索引擎Elasticsearch(民间的)与Solr(阿帕奇的又被干掉了)

01、配置pom.xml.